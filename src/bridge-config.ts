import "dotenv/config"; import {z} from "zod"; const a=z.string().regex(/^0x[a-fA-F0-9]{40}$/);
export function bridgeConfig(){return z.object({kiteRpc:z.string().url(),arbitrumRpc:z.string().url(),kitePyusd0:a,arbitrumPyusd0:a,kiteEndpoint:a,arbitrumEndpoint:a,kiteEid:z.coerce.number().int().positive(),arbitrumEid:z.coerce.number().int().positive()}).parse({kiteRpc:process.env.KITE_RPC_URL,arbitrumRpc:process.env.ARBITRUM_RPC_URL,kitePyusd0:process.env.KITE_PYUSD0_ADDRESS,arbitrumPyusd0:process.env.ARBITRUM_PYUSD0_ADDRESS,kiteEndpoint:process.env.KITE_LAYERZERO_ENDPOINT,arbitrumEndpoint:process.env.ARBITRUM_LAYERZERO_ENDPOINT,kiteEid:process.env.KITE_LAYERZERO_EID,arbitrumEid:process.env.ARBITRUM_LAYERZERO_EID});}
